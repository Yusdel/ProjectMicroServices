'use strict';

/**
 * Jasmin test class
 */
describe('Sacrum.Home module', function () {

  beforeEach(module('Home'));

  describe('Home controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var HomeCtrl = $controller('HomeCtrl');
      expect(HomeCtrl).toBeDefined();
    }));

  });
});